<h1>Add New Task</h1> 
 
<%= form_for [@person, @task] do |t| %>
  <%= t.label :title %>
  <%= t.text_field :title %>
  <br />
  <%= t.label :task %>
  <%= t.text_area :task %>
  <br /> 
  <%= t.submit %> 
  <% end %> 
  <%= link_to "Back", person_path(@person.id) %>
  <%- #<% %> my comment -%>